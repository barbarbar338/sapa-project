use crate::globals;

/*
    TODO: Implement BPM detection
    Detect the Heart Rate (BPM) from 5 seconds of heart sound voltage signal
*/
pub fn bpm(_signal: Vec<f64>) -> f64 {
    let _sample_rate = *globals::SAMPLE_RATE;

    64.0
}
